# Application
PORT=3000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/vestitus

# JWT
JWT_SECRET=your-secret-key-change-this
JWT_EXPIRATION=24h

# Email (Resend)
# Sign up at https://resend.com to get your API key
RESEND_API_KEY=re_your_api_key_here
# Optional: Custom from email (must be verified domain or use default)
EMAIL_FROM=Vestitus <onboarding@resend.dev>

# CORS
CORS_ORIGIN=*
# For production, use specific origins like: http://localhost:3000,https://yourdomain.com

# OAuth - Google (Token Verification)
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# Configure OAuth 2.0 Client ID for iOS/Android/Web applications
GOOGLE_CLIENT_ID=your-google-client-id

# OAuth - Apple (Token Verification) 
# Create at https://developer.apple.com/account/resources/identifiers/list/serviceId
# Configure Sign In with Apple for your iOS/Android app
APP_BUNDLE_ID=your-apple-service-id

